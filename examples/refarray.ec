public class nRefArray(Array) {
  hidden nRefArray();   
  public nRefArray(=length);
  hidden nRefArray(=capacity, =dataType, =dataSize);
  hidden nRefArray(=capacity, =dataType, =dataSize, =values);

  void setObject(u64 index, num object) {}
}
